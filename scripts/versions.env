# Every pinned tool version. Sourced by the scripts and by CI.

# The toolchain lints run under. Not the minimum users need; that is
# rust-version in Cargo.toml.
RUST_VERSION=1.98.1
CARGO_DENY_VERSION=0.20.2
GIT_CLIFF_VERSION=2.14.1
GITLEAKS_VERSION=8.30.1
